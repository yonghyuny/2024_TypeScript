function add(x: number, y: number): Number {
  return x + y;
}

const add1 = (x: number, y: number): number => {
  return x + y;
};

const isCheck = (x: String): boolean => {
  return !!x;
};

const minus = (x: number, y: number): number => x - y;

console.log(minus(10, 5));
